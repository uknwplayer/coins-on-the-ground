name: Liquity V2 Read-Only Scout

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "src/coins_on_the_ground/scouts/liquity_v2.py"
      - "tests/test_liquity_v2.py"
      - ".github/workflows/liquity-v2-scout.yml"
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: read

concurrency:
  group: coins-liquity-v2-read-only
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install
        run: python -m pip install .

      - name: Scan Liquity V2 liquidations without signing or broadcasting
        env:
          LIQUITY_RPC_URL: ${{ vars.LIQUITY_RPC_URL }}
        run: |
          mkdir -p data
          python -m coins_on_the_ground.scouts.liquity_v2 \
            --batch-size 500 \
            --max-troves-per-branch 5000 \
            --output data/liquity-v2-scan.json

      - name: Upload read-only scan
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: liquity-v2-read-only-scan
          path: data/liquity-v2-scan.json
          if-no-files-found: error
          retention-days: 7
